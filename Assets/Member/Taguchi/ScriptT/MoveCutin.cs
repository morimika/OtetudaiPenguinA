using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MoveCutin : MonoBehaviour
{
    [SerializeField,Header("カットイン用ペンギン")] public GameObject Pengin;
    [SerializeField, Header("カットイン背景")] public GameObject BackGround;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
